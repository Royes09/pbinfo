#include <iostream>

using namespace std;
long nr_cif_zero(long n)
{
    if (n==0)
        return 1;
    int nr=0;
    while (n)
    {
        if (n%10==0)
            nr++;
            n=n/10;
    }
    return nr;
}
