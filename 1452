#include <iostream>

using namespace std;

int main()
{
    int n,p,X[1501],i;
    cin>>n>>p;
    for(i=1;i<=n;i++)
        cin>>X[i];
    for(i=p;i<n;i++)
        X[i]=X[i+1];
    for(i=1;i<n;i++)
        cout<<X[i]<<" ";
    return 0;
}
