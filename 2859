#include <iostream>

using namespace std;
int ok;
int TreiCifImp(int n)
{
    while (n)
    {
        if(n%2==1 && (n/10)%2==1 && (n/100)%2==1)
            ok=1;
            n=n/10;
    }
        if(ok==1)
            return 1;
        else return 0;
}
